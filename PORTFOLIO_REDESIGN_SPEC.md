# 个人作品集网站深度改版 Spec

> 面向一次可持续数小时的自主代码 Session。执行目标不是“增加更多特效”，而是在保留现有票根开场和 MacBook 项目桌面这两个识别点的基础上，把网站升级为一份更完整、更克制、更有产品判断力的 AI 产品经理作品集。

## 0. 给执行 Session 的直接指令

在 `/Users/yvaine/Documents/current/resume/web-vue` 中完成本 Spec。

开始前：

1. 阅读仓库 `../AGENTS.md`、本 Spec、`../PROJECTS.md`、`../INTERVIEW_QA.md`、`../自我介绍.md`。
2. 运行当前站点，在桌面和移动端分别保存基线截图。
3. 先完成参考案例矩阵和页面结构草图，再开始大范围改代码。
4. 不虚构项目事实、指标、客户信息或内部架构细节。没有确认的数据宁可不写，或标记为内容待确认，不得编造“提升 xx%”。
5. 不因为追求“丰富”而堆叠玻璃拟态、霓虹渐变、大量圆角卡片、满屏粒子或滚动劫持。
6. 完成后必须执行 `npm run build`，并在浏览器中验证桌面、移动端、键盘操作和 reduced-motion。

本 Session 可以自主完成所有已明确的设计和实现，不要每做一个小改动就停下来询问。只有涉及项目事实、公司保密边界、音乐版权或缺失的个人信息时才需要停下确认。

### 本轮范围裁决

以下三条是最新需求，优先于本文中引用的旧仓库说明：

1. MacBook 中展开的四个项目并列呈现，不设置默认主项目或阿里云优先叙事。
2. `基于 AI 的跨工艺集成电路行为建模`只保留在时间轴，不进入项目详情。
3. 爱好页本轮暂缓，不创建、不挂载、不验收；现有素材保持原样。

---

## 1. 项目目标

### 1.1 核心目标

- 第一眼让访问者知道：这是叶奕含的个人作品集，目标方向是 AI / 数据库产品经理。
- 清楚呈现四个并列的重点项目/经历，让访问者理解它们各自的问题、个人职责和方法。
- 在不牺牲阅读效率的前提下，增加有目的的交互、悬停和滚动反馈。
- 所有已经出现在页面上的区域都有完整文案，不再出现空白纸张、占位文案或“后续补充”式展示。
- 让网站体现“问题定义 → 场景拆解 → 技术选型 → 数据/评估迭代”的产品方法，而不是把 AI 当作装饰。

### 1.2 设计关键词

`Editorial`、`Restrained`、`Technical`、`Tactile`、`Confident`

对应中文：

- 编辑感：像一份经过策展的作品档案，而不是组件库 Demo。
- 克制：留白、细线、字体层级优先，色彩和动效只服务重点。
- 技术可信：架构和流程可读、边界清楚，不画“看起来很 AI”的假图。
- 有触感：票根、文件夹、MacBook、纸张这些已有物理隐喻继续保留。
- 大气：四个重点项目保持同级，通过留白、秩序和内容本身建立可信度，而不是争抢视觉注意力。

### 1.3 非目标

- 不重写成一个完全不同的模板站。
- 本轮不制作或接入爱好页；现有 Three.js、GLB、照片和爱好相关代码留待后续单独重新设计。
- 不增加登录、CMS、后台、评论、访客统计等未要求功能。
- 不把所有内容都塞进卡片或弹窗。
- 不为了“高级感”牺牲文字可读性、原生滚动和移动端体验。

---

## 2. 当前基线与必须保留的资产

### 2.1 当前问题

- `TransitionPage.vue` 同时承担票根、作品集背景、时间轴、技术栈和调试工具，文件过大且职责混杂。
- 现有时间轴被压缩在半屏纸张内，多段经历都变成小圆角块，文字密、可读性弱。
- 时间轴页面左侧存在大面积没有实际内容的纸张区域。
- `ZaiPage.vue` 已有可用的 MacBook 桌面和项目窗口，但四个项目都需要更完整的内容结构与各自适合的展示形式。
- `TransitionPage.vue` 和 `ZaiPage.vue` 都超过合理的单组件规模，需要按页面职责拆开，但不能做无关重构。

### 2.2 必须保留

- 票根撕开/拖动的开场概念。
- `TransitionPage` 的 `transition-complete` 事件契约。
- `public/macbook-frame.png` 作为 MacBook 稳定视觉锚点。
- MacBook 桌面点击文件夹、窗口左上角红点关闭的交互模型。
- 智谱工作流节点可以拖动、平移、缩放、查看说明；不增加节点编辑。
- 智能缺陷分析系统的真实演示视频。
- Smart Ganttodo 的在线体验入口。
- 已有架子鼓、缝纫机 GLB 和爱好照片资产不得删除或重命名，但本轮不将它们接入页面。

### 2.3 仅保留在时间轴的项目

- `基于 AI 的跨工艺集成电路行为建模 / 创“芯”大赛 EDA 精英挑战赛全国三等奖`保留为时间轴条目。
- 它不进入 MacBook 文件夹、项目导航、项目总览和详情窗口，不为它设计展开交互。
- 时间轴只写时间、项目名、角色和一句结果，不延伸成长案例。
- `../PROJECTS.md` 继续保留其历史事实。

---

## 3. 参考学习任务

实现前新建 `docs/design-reference-matrix.md`，至少收集 8 个案例：

- 国内或中国团队来源不少于 3 个。
- 海外来源不少于 4 个。
- 至少包含：作品集、企业产品网站、动效规范、长滚动叙事各一类。

每个案例记录：

| 字段 | 要求 |
|---|---|
| 名称与 URL | 直接链接 |
| 截图 | 首页或关键交互状态 |
| 可借鉴 | 最多 3 条，具体到布局/动效/导航 |
| 不照搬 | 至少 1 条，说明风险 |
| 对应本项目 | 明确落到哪个页面或交互 |

### 3.1 种子参考

以下是起点，不是要求逐像素模仿：

- [Ant Design Motion](https://ant.design/docs/spec/motion-cn/)  
  学习“自然、高效、克制”的动效判断；每个动效都要回答“是否帮助理解或反馈”。
- [Ant Design 设计价值观](https://ant.design/docs/spec/values/)  
  学习少量元素表达重点、保持确定性和一致性。
- [TDesign](https://static.tdesign.tencent.com/)  
  学习国内企业级产品的稳定层级和清晰信息组织。
- [阿里云 DMS Data Copilot](https://www.alibabacloud.com/help/zh/doc-detail/2557576.html)  
  只用于理解数据库 AI 产品的公开术语和信息层级，不得把公开文档内容冒充个人工作成果。
- [范天欣 UX/UI 作品集](https://lilithworks.com/)  
  学习“能力证据 + 重点案例 + 设计思考”的内容结构，避免只列工具名。
- [Linear](https://linear.app/homepage)  
  学习大标题、精确留白、细线、产品图和滚动节奏；不要复制黑色渐变和发光效果。
- [Rauno Freiberg](https://rauno.me/)  
  学习细节型微交互、状态反馈和克制的个人表达。
- [Dennis Snellenberg](https://dennissnellenberg.com/work)  
  学习项目悬停预览、指针语义和大字排版；避免过度接管滚动。
- [Bruno Simon](https://bruno-simon.com/)  
  学习“交互本身成为个人记忆点”；不要把本项目改成重型 3D 游戏。
- [AG-UI 官方文档](https://docs.ag-ui.com/)  
  用于校准 AG-UI 是 Agent 与前端之间的事件驱动交互协议，架构图应表现双向、流式和状态更新，而不是普通 REST 箭头。
- [Hermes Agent 官方仓库](https://github.com/NousResearch/hermes-agent)  
  只用于校准 Skills、Memory、自我改进等公开概念；内部运维项目仍以用户提供的真实工作为准。

### 3.2 研究结论必须转成约束

参考阶段结束后先写 5-8 条本项目自己的原则，例如：

1. 一个视口只设一个主要视觉动作。
2. 文字信息用排版和分隔线组织，不默认放进圆角卡片。
3. Hover 必须说明“这里可以做什么”，不是让所有元素漂浮。
4. 页面滚动保持用户控制，不使用 scroll-jacking。
5. 四个重点项目使用相同入口规格和视觉权重，各自内部再选择适合内容的展示方式。

---

## 4. 新的信息架构

页面顺序：

1. `Cover / Ticket`
2. `Profile / About + Capability Stack`
3. `Experience Timeline`
4. `Projects / MacBook Workspace`

### 4.1 导航

过渡完成后出现低存在感的固定导航：

`ABOUT / TIMELINE / WORK`

- 桌面端使用纯文字、细线和当前章节小圆点，不做大胶囊导航。
- 移动端使用简洁的菜单按钮，展开后仍保持同样的直线与文字语言。
- 当前章节随滚动更新。
- 支持点击锚点和键盘访问。

### 4.2 滚动策略

- 票根阶段可以继续锁定滚动以完成过渡。
- 进入作品集后使用正常、连续的滚动。
- 删除或限制当前全局 `scroll-snap`，尤其不能让长时间轴被强制吸附。
- 不引入平滑滚动劫持库。
- `scroll-behavior: smooth` 只用于用户主动点击导航，且 reduced-motion 时关闭。

---

## 5. 页面级设计与文案

## 5.1 Cover / Ticket

### 目标

保留最有辨识度的“撕票进入个人空间”，但让引导更直接、完成更快。

### 文案

- 主身份：`叶奕含`
- 方向：`AI / DATABASE PRODUCT MANAGER`
- 辅助说明：`Portfolio · 2026`
- 交互引导：`拖动票根，进入作品集`
- 键盘提示：`也可以按 Enter`

### 交互

- 首次轻微提示票根可拖动，不做无限循环抖动。
- 拖动时票根与指针保持直接跟随。
- 放手后的飞行和落位保留，但总时长控制在约 1.2-1.8 秒。

## 5.2 Profile / About

### 页面形式

继续使用票根落入笔记本/纸张的物理场景，但把当前拥挤时间轴移走。

- 左页：个人简介与定位。
- 右页：四组能力，不做四张圆角卡片；使用编号、细线、关键词和一条证据。
- 便利贴只保留为少量强调，不承担大段文字。
- 当前空白纸张必须被真实内容使用。

### 主标题

`把复杂问题，拆成可以被验证的产品系统。`

### 简介文案

> 我享受从复杂、抽象的问题里拆出清晰的用户任务、业务链路和产品方案。能写 Prompt、画原型，也会自己动手写代码验证想法——不是为了证明什么都会，而是不想让判断只停留在“我觉得”。
>
> 我相信好的 AI 产品不是为了把 AI 塞进流程，而是先定义问题、拆解场景，再决定哪些交给模型，哪些必须由确定性流程、数据和评估兜底。
>
> 现在，我在阿里云数据库团队做产品经理实习，继续把这套方法放进更复杂、更真实的产品与运维场景里。

### 能力结构与文案

**01 / PRODUCT JUDGMENT**  
问题定义、场景拆解、PRD、原型、需求优先级。  
`把“想做一个 AI 功能”转成明确用户、任务链路、边界与验收方式。`

**02 / AGENT & WORKFLOW**  
Prompt、Skills、Agent、Workflow、AG-UI、记忆与评估。  
`判断模型自由度与确定性流程的边界，并通过 badcase 和回归持续优化。`

**03 / AI & ENGINEERING**  
Python、PyTorch、CV、API 集成、Docker。  
`能够读懂系统与模型约束，也能做出可运行原型验证产品判断。`

**04 / PROTOTYPE & FRONTEND**  
Vue、JavaScript、Figma、Axure、Git、Vibe Coding。  
`把需求快速变成可体验、可讨论、可测试的界面，而不是停在文档。`

### 辅助信息

- `浙江大学 · 电子信息硕士（2027）`
- 期望Base： 杭州。
- 邮箱：`yhye@zju.edu.cn`。
- GitHub：`https://github.com/yva1nne`（上线前再核对链接可访问）。
- 若提供简历下载，只使用当前确认版本并复制为稳定、无中文空格的公开文件名。

个人简介、技术栈和联系方式都在第二页完成，不在网站结尾重复创建 Contact/Footer 页面。未确认的其他联系方式不得猜测。

## 5.3 Experience Timeline

### 页面目标

把原半屏时间轴改成一个独立的长滚动叙事页，突出经历之间的转折和能力积累。

### 页面形式

- 总高度建议 `300-450vh`，根据内容自然形成，不硬凑长度。
- 桌面端左侧是 sticky 年份与进度线，右侧是经历正文。
- 每段经历使用大日期、标题、角色、1 句定位和 2-3 条证据。
- 不使用整块圆角矩形包裹每段经历；主要依靠留白、直线、缩进和字号。
- 当前经历使用与其他条目一致的 `NOW` 状态标记，不给阿里云额外扩大字号、色块或版面。
- 滚动到当前条目时，年份、进度线和正文同步激活。
- 移动端变成单列，进度线靠左，正文完整可读。

### 时间轴内容

**2020.09 — 2024.06**  
`浙江大学 · 本科`  
电子科学与技术。建立电子信息与工程技术基础。

**2024.09 — 至今**  
`浙江大学 · 硕士`  
电子信息。研究与项目逐步从算法验证延伸到产品定义、系统集成与真实场景应用。

**2024.09 — 2025.07**  
`智能缺陷分析系统`  
产品 + 核心开发成员。面向半导体产线 YE 工程师 RCA 阶段，整合分类、分割、语义和根因检索；主责 DRNet 检索、GUI 与系统集成。

**2025.08 — 2025.12**  
`基于 AI 的跨工艺集成电路行为建模`  
核心开发成员。参与跨工艺参数推演与工程化交付，项目获创“芯”大赛 EDA 精英挑战赛全国三等奖。该项目仅在时间轴出现，不提供详情入口。

**2025.09 — 2025.12**  
`Smart Ganttodo`  
AI 产品经理 / 独立开发。验证自然语言多意图解析、结构化 actions、甘特图状态与拖拽修正的个人任务规划链路。

**2025.12 — 2026.05**  
`智谱华章 · AI 产品经理实习`  
把复杂 ToB AI 项目中的分散材料、展示目标和自然语言交互需求，组织成可讲、可演、可追问的 LLM-driven workflow。

**2026.06 — 至今**  
`阿里云 · 数据库产品经理实习`  
围绕数据库 AI 助手与内部运维 AI 助手，推进功能设计、Skills 优化、测试迭代、平台接入与商业化落地。

### 动效

- 进度线随真实滚动增长。
- 条目进入视口时使用 `opacity + translateY(12-20px)`，只播放一次。
- 年份切换使用颜色和字重，不用整屏闪白或大幅缩放。
- 避免连续密集的逐字动画。

## 5.4 Projects / MacBook Workspace

### 项目顺序

MacBook 桌面文件夹必须按以下顺序：

1. `阿里云数据库实习`
2. `智谱华章实习`
3. `智能缺陷分析系统`
4. `甘特图风格化待办系统`

这一列表由本次用户需求明确覆盖仓库旧说明中的“三个文件夹”。排序只是目录顺序，不表达项目等级。进入项目页时仍先显示桌面，不默认打开阿里云或其他项目。

- 四个文件夹使用同样的尺寸、标签层级、悬停反馈和点击行为。
- 四个项目在总览、外部说明区和导航中保持同等视觉权重。
- 四个项目继续复用同一套浏览器窗口 chrome，内部内容可使用不同的展示形式。
- 不给阿里云增加 `CURRENT` 徽章、独占尺寸、特殊默认打开状态或额外醒目的入口。

### 项目总览文案

标题：`四段工作，四种把 AI 变成产品的方法。`

正文：

> 从数据库产品与运维 Agent，到 ToB 展示工作流、工业缺陷分析和个人效率工具，我更关心的不是“用了什么模型”，而是用户要完成什么任务、系统如何分工，以及结果怎样被验证。

### 通用项目详情结构

每个项目在 MacBook 内保持同一浏览器窗口 chrome，但内容可以有不同的核心交互。

右侧说明不再只用 `角色 / 问题 / 验证 / 看点` 的固定模板，改为：

1. 一句话定位
2. 我的职责
3. 核心问题
4. 方案/系统
5. 结果与证据
6. 边界/复盘

若内容较多，使用锚点或文本 tab，不能把全部信息隐藏在 hover 中。

## 5.5 阿里云数据库产品经理实习

### 总体定位

Kicker：`2026.06 — NOW / ALIBABA CLOUD`

标题：`让数据库 AI 从“能回答”，走向“能工作”。`

摘要：

> 在阿里云数据库产品经理实习中，我围绕两条主线工作：一条面向数据库 AI 助手，参与功能设计、Skills 优化与商业化落地；另一条面向内部运维 AI 助手，通过测试和方案优化，把云端部署的 Hermes 以 AG-UI 协议接入内部运维平台，探索更高效的数据库维护、可沉淀的运维经验，以及 Harness 架构的持续自我优化。

角色：`数据库产品经理实习生`

内容安全说明：

- 只展示可公开、已脱敏的产品逻辑。
- 不出现客户名、内部平台真名、账号、数据、未公开截图和敏感架构参数。
- 没有确认的业务指标不展示数字。

### 展示形式：双主线 Product System

进入阿里云文件夹后，先看到一张不是卡片墙的“双主线系统图”：

```text
数据库场景
   │
   ├── 01 数据库 AI 助手
   │      功能设计 → Skills 优化 → 场景验证 → 商业化落地
   │
   └── 02 内部运维 AI 助手
          内部运维平台 ⇄ AG-UI ⇄ 云端 Hermes
                              ├─ Tools / Skills
                              ├─ Memory / Experience
                              └─ Harness Improvement
```

视觉要求：

- 用细线、节点、状态文字和少量橙色表达系统，不画霓虹“AI 大脑”。
- 两条主线通过文本 tab 或点击线路切换。
- Hover 节点时给出短说明；点击节点打开右侧详情面板。
- 线路上可以有低速事件流点，表达 AG-UI 的双向流式通信；它是辅助说明，不可喧宾夺主。
- reduced-motion 下事件流停止，结构仍然完全可读。

### 主线 A：数据库 AI 助手

标题：`把模型能力组织成用户愿意使用、产品能够交付的功能。`

介绍文案：

> 这条工作不是单纯增加一个聊天入口，而是从数据库用户的真实任务出发，定义助手在不同环节应该理解什么、调用什么能力、怎样给出可执行结果，再通过 Skills 和评测持续提高稳定性，最终连接到可交付、可商业化的产品场景。

建议用以下四步组织页面。除用户已明确提供的“功能设计、Skills 优化、商业化落地”外，其余步骤和方法只可作为待核对的展示维度，不得默认写成个人已完成的工作：

**01 功能设计**  
从用户任务、入口、上下文和操作风险出发，明确 AI 助手的功能边界与交互链路。

**02 Skills 优化**  
围绕数据库领域 Skills 做优化。触发条件、执行步骤、工具调用、输出格式、失败兜底等是可供核对的候选维度；页面只保留实际做过且可公开的部分。

**03 场景验证**  
说明 Skills 如何进入目标场景，以及如何判断它是否更可用。测试集、badcase、回归或质量指标只有在用户确认后才能出现。

**04 商业化落地**  
展示从能力到商业场景的产品思路，关注“谁使用、解决什么问题、以什么产品形态承载”。客户、版本、交付路径和结果只使用可公开事实。

商业化部分不得虚构收入、客户数或转化率。若缺少公开结果，展示“从能力到场景”的方法和已确认产物。

### 主线 B：内部运维 AI 助手

标题：`让 Agent 进入真实运维系统，并把经验留在系统里。`

介绍文案：

> 我参与内部运维 AI 助手的测试和优化，并协同将云端部署的 Hermes 通过 AG-UI 接入内部运维平台。重点不只是完成一次问答，而是让 Agent 能在平台上下文中持续接收状态、调用能力、返回过程与结果，同时把有效经验沉淀为可复用的记忆和 Skills。

建议交互演示：

1. 用户在运维平台发起一个脱敏后的示例任务。
2. AG-UI 时间线可使用真实事件族的脱敏示意，例如 `RUN_STARTED`、`TEXT_MESSAGE_START/CONTENT/END`、`TOOL_CALL_START/ARGS/END`、`STATE_DELTA`、`RUN_FINISHED`。
3. Hermes 侧显示 Skills、工具和记忆被按需调用。
4. 任务完成后，一条“经验候选”进入沉淀与评测流程。
5. 用户可切换“本次执行 / 经验沉淀 / Harness 优化”三个视角。

说明：

- 以上是前端解释模型，不得伪装成真实线上监控台。
- 事件名只使用与实际实现一致、可公开的部分；如果只是产品解释标签，不得伪装成协议原始事件。
- “Harness 自我优化”的具体闭环必须根据真实工作补齐；若尚不能披露，用抽象层表达：
  `发现 badcase → 提炼经验/Skill → 回归验证 → 受控更新`。

### 阿里云待确认内容

在实现中集中维护为内容清单，不要散落猜测：

- 数据库 AI 助手具体面向哪些用户角色。
- 可以公开的功能名称和 Skills 示例。
- 个人独立负责、参与、协同的准确边界。
- 可公开的测试集规模、质量变化、效率变化或商业化结果。
- 内部运维平台、Hermes 部署和 Harness 结构可披露到什么粒度。

## 5.6 智谱华章实习

Kicker：`2025.12 — 2026.05 / ZHIPU AI`

标题：`把复杂 ToB AI 项目，组织成可讲、可演、可追问的系统。`

摘要：

> 围绕城智大模型大屏，把语音入口、ASR 纠错、意图路由、页面跳转、场景讲解和问答子流程串成展示型 LLM-driven workflow。

个人职责：

- 主导 5 个大屏、14 个页面的展示方案、内容组织和演示脚本。
- 设计智能中控、智能讲解、智能问答三类能力。
- 设计 ASR 纠错、Router、场景 QA、澄清、拒答和兜底链路。

边界：

> LLM 负责自然语言理解、路由匹配和受控生成；Workflow 负责确定性跳屏、讲解流程、问答子链路、兜底、日志和回归。它不是完全自治 Agent，也不包装成底层模型训练项目。

前端形式：

- 保留现有节点画布。
- 增加“问题 / 系统 / 我的工作 / 边界”四个阅读锚点。
- 默认展示完整架构，点击节点才展开细节。
- 不增加节点编辑和持久化。

## 5.7 智能缺陷分析系统

Kicker：`2024.09 — 2025.07 / INDUSTRIAL AI`

标题：`把四个模型结果，组织成工程师可判断的辅助报告。`

摘要：

> 面向半导体产线 YE 工程师 RCA 阶段，整合缺陷分类、分割、语义描述和历史根因检索，帮助工程师快速理解缺陷并缩小排查范围。

个人职责：

- 产品方案与系统交互。
- 主责 DRNet 根因检索模块。
- GUI 搭建、系统集成和 Top-K 相似缺陷结果交互。

结果：

- 使用 2000+ 张真实产线数据完成验证。
- 检索模块单次时延 0.03s。
- Rank-1 准确率 96.2%。
- 获“华为杯”中国研究生人工智能创新大赛全国一等奖。

边界：

> 系统不自动给出最终根因，而是把分类、位置、形貌和历史相似案例组织成辅助信息，交给 YE 工程师判断。

前端形式：

- 保留演示视频。
- 视频旁增加四段式输出索引：`分类 / 分割 / 语义 / 检索`。
- 播放进度到对应阶段时可高亮标签；若实现成本过高，使用用户点击标签跳到预设时间点。
- 不再使用“视频文件占位”作为可见状态。

## 5.8 Smart Ganttodo

Kicker：`2025.09 — 2025.12 / INDEPENDENT MVP`

标题：`用自然语言生成计划，再用甘特图把它改对。`

摘要：

> 一个轻量个人工作流 MVP：用自然语言降低任务创建和调整成本，用甘特图表达跨天、并行和长期任务，并保留拖拽修正的用户控制。

核心链路：

`自然语言输入 → 多意图识别 → JSON actions → Gantt state → 拖拽编辑`

关键判断：

- Prompt 约束 + 日历参照表 + 时区锁定，降低时间推理错误。
- AI 生成的是可编辑初稿，不是不可修改的最终答案。
- 当前定位是个人 MVP，不包装成完整项目管理工具。

前端形式：

- 保留 iframe 在线体验。
- 加载失败时显示项目截图、核心链路和“新窗口打开”链接，不只显示网络提示。
- 在 iframe 外提供一个真实示例输入及对应 actions，访问者不操作也能看懂项目。

## 6. 全局音乐播放器

### 行为

- 不自动播放。
- 使用本地、用户有权使用的音频文件；不热链第三方音乐。
- 播放器必须在最终页面可见且能够真实播放。若仓库没有合法音源，使用有明确来源与许可的 CC0/公共领域音乐并记录来源，或停下来向用户索取音频；不能用隐藏组件或静音空壳通过验收。
- 控件：播放/暂停、曲名、进度、音量/静音。
- 记住音量和上次是否静音，不跨刷新自动恢复播放。

### 视觉

- 固定在页面边缘，使用细线、文本和真实图标。
- 不做大号圆角胶囊。
- 播放时可有很小的电平/进度反馈，不做满屏可视化。
- 在 MacBook 和移动端不能遮挡主要内容。

### 可访问性

- 所有按钮有可读 label。
- 支持 Space/Enter。
- 进度条可键盘调整。
- reduced-motion 时移除循环波形动画。

---

## 7. 自定义鼠标

### 桌面端

- 仅在 `pointer: fine` 且 `hover: hover` 时启用。
- 默认形态：小实心点 + 极细外环，颜色根据背景确保可见。
- 可交互元素状态：
  - 文件夹/项目：`OPEN`
  - 工作流画布：`DRAG`
  - 音乐播放器：`PLAY`
  - 外链：`VISIT`
- 文本状态只在有意义的区域出现，不要每个按钮都显示。

### 约束

- 只有自定义 cursor 成功挂载后才对页面设置 `cursor: none`。
- 位置更新使用 `requestAnimationFrame + transform`。
- 不读取或追踪用户行为。
- 触屏、粗指针、reduced-motion 下关闭跟随动画并恢复系统指针。
- 键盘焦点必须独立清晰，不能依赖鼠标状态。

---

## 8. 动效系统

### 8.1 动效层级

**Level 1 / Feedback：120-180ms**  
按钮按下、hover、图标状态、文字下划线。

**Level 2 / Component：220-320ms**  
tab 切换、详情面板、项目窗口内容切换。

**Level 3 / Section：420-700ms**  
章节进入、时间轴条目、MacBook 开合。

**Signature：1200-1800ms**  
只用于票根进入作品集。

### 8.2 统一原则

- 进入稍慢，退出更快。
- 位移小于 24px，除非表达明确的空间关系。
- 一屏只保留一个主要 motion story。
- 列表 stagger 间隔 40-80ms，最多 6 个元素。
- 常规动画优先 `transform` 和 `opacity`。
- `will-change` 只在即将动画的元素上短暂使用。

### 8.3 Hover 清单

- 文字链接：下划线从左到右展开。
- 项目文件夹：上移 3-5px、图标高光变化、名称加深。
- 时间轴条目：左侧规则线加粗或延长。
- 项目节点：边框/背景轻微变化，指针提示其动作。
- 照片：最大 `scale(1.015-1.025)`，不能大幅放大。
- 禁止给所有块统一套 `translateY(-8px) + 大阴影`。

### 8.4 Reduced motion

必须实现 `@media (prefers-reduced-motion: reduce)`：

- 关闭视差、跟随指针、循环动画和大范围缩放。
- 章节转场改为短淡入。
- 票根提供按键直接进入。
- 所有内容和功能仍可访问。

---

## 9. 视觉系统

### 9.1 构图

- 使用明确的 12 列桌面网格和 4 列移动网格。
- 通过字号、留白和细线建立层级。
- 内容最大宽度保持一致，不让各页像不同模板拼接。
- 物理对象（票、纸、MacBook）可以有真实阴影；普通 UI 不使用厚重浮层阴影。

### 9.2 色彩

- 主背景：暖灰/纸张白。
- 主文字：接近黑色的暖墨色。
- 次级文字：中性灰。
- 通用强调：沿用当前深蓝，但降低饱和度。
- 各项目详情内部可以使用一种小面积语义色；阿里云如使用经确认的品牌橙，只限其详情内容，不改变文件夹、导航或总览层级。
- 每个视口最多一个高注意力强调色。

### 9.3 字体

- 中文正文优先 `Noto Sans SC`。
- 英文、数字或大标题可以继续使用 `Fraunces + Manrope` 的组合。
- 不为了装饰大量使用全大写；全大写只用于 kicker、编号和导航。
- 正文桌面端建议 16-18px，移动端不小于 15-16px。

### 9.4 圆角

- 普通内容区默认无圆角。
- 小控件可使用 2-6px。
- 浏览器窗口、MacBook 屏幕按真实对象保留圆角。
- 禁止用 16-24px 圆角卡片作为所有内容的默认容器。

---

## 10. 内容与数据架构

将页面内容从超大 Vue 文件中分离，但只做必要拆分：

```text
src/
  data/
    portfolio.js
  components/
    GlobalNav.vue
    MusicPlayer.vue
    CustomCursor.vue
    timeline/
      TimelineItem.vue
    projects/
      MacBookWorkspace.vue
      AliyunProject.vue
      ZhipuProject.vue
      DefectProject.vue
      GanttodoProject.vue
  views/
    TransitionPage.vue
    TimelinePage.vue
    ZaiPage.vue
```

说明：

- `TransitionPage.vue` 保留票根状态机和落位场景，不再承载长时间轴。
- `ZaiPage.vue` 保持直接，负责项目选择与 MacBook 外层；各项目复杂内容拆成子组件。
- 本轮不创建、挂载或重做 `HobbyPage.vue`。这一最新需求覆盖仓库旧说明中的爱好页挂载顺序；现有爱好素材保持原样。
- 项目文案、时间轴、技能项集中在 `src/data/portfolio.js`，避免同一日期在多个组件里漂移。
- 不创建通用 CMS、复杂 schema 或无实际复用的抽象。
- `useTicketTransition.js` 的公共行为不随意重写。
- 自定义 cursor、滚动可见性和音乐分别放在独立组件/组合式函数中。

### 文档同步

实现中发现新事实时：

- 稳定项目事实同步到 `../PROJECTS.md`。
- 高频项目面试问答同步到 `../INTERVIEW_QA.md`。
- 纯网站设计、动效和实现记录只写进 `docs/`，不要污染项目事实库。
- 阿里云新增事实在没有用户确认前不要扩写成面试成果。
- 本次已经确认的两项时间事实必须同步：智谱改为 `2025.12 - 2026.05`；新增阿里云 `2026.06 - 至今`。同时在 `../PROJECTS.md` 增加阿里云目前已确认的两条工作主线，并检查 `../INTERVIEW_QA.md` 中跨项目总述是否仍一致。

---

## 11. 响应式与可访问性

### 11.1 断点

- `> 1080px`：完整桌面布局。
- `761-1080px`：缩小间距和字号，MacBook 与说明改为上下布局。
- `<= 760px`：单列，时间轴左线，导航折叠；MacBook 保持视觉锚点，项目说明放在其下方并确保文字无需在微缩屏幕中阅读。

### 11.2 必须验证

- 390 × 844
- 768 × 1024
- 1280 × 800
- 1440 × 900
- 宽屏 1728px 以上不能无限拉宽。

### 11.3 可访问性

- 所有交互可通过键盘完成。
- `:focus-visible` 清晰且不被 overflow 裁掉。
- 颜色不是唯一状态提示。
- 图片有合适 alt；纯装饰图 `alt=""`。
- video 有可理解标题，后续有条件时增加字幕/文字摘要。
- 音频不自动播放。
- 自定义鼠标不影响辅助技术。

---

## 12. 性能与实现约束

- 默认使用 Vue、CSS、Web Animations API、IntersectionObserver、requestAnimationFrame。
- 不为简单 reveal 引入大型动效库。
- 如确实需要新增依赖，先记录原因、体积和原生方案为什么不足。
- 大图使用合适尺寸、现代格式和 lazy loading。
- 视频使用 `preload="metadata"`。
- 首屏不预加载项目视频正文、iframe 或非首屏大图。
- 动画避免 layout thrashing；优先 transform/opacity。
- 浏览器 console 不得有未处理错误。
- iframe 加载失败必须有可用 fallback。

性能目标：

- 首屏没有明显卡顿或白屏。
- 桌面端常规滚动接近 60fps。
- 移动端不会因为视频、iframe 或大图导致页面长时间失去响应。
- 生产构建无 warning；若第三方 iframe 有不可控 warning，单独记录。

---

## 13. 实施阶段

### Phase 0 — Baseline 与参考

- 启动现有项目。
- 保存桌面/移动端基线截图。
- 完成 `docs/design-reference-matrix.md`。
- 输出一页结构草图和动效清单。

验证：每个参考都能落到具体页面，不存在“仅因为好看”的参考。

### Phase 1 — 内容模型与页面骨架

- 建立 `portfolio.js`。
- 更新 App 页面顺序。
- 将时间轴从 `TransitionPage.vue` 拆出。
- 将跨工艺建模项目收敛为仅在时间轴出现的条目，不创建项目详情入口。
- 新增阿里云，与智谱、缺陷系统、Ganttodo 组成四个并列的 MacBook 项目。
- 不创建或接入爱好页。

验证：无动效时，所有页面和文案已经完整可读。

### Phase 2 — 视觉基础

- 统一网格、字体、颜色、间距、边框与圆角规则。
- 完成 About 与长时间轴。
- 保留票根和 MacBook 的物理质感。

验证：关闭动画后仍然简约、完整、大气。

### Phase 3 — 四个并列项目

- 完成阿里云双主线，以及 AG-UI / Hermes / Memory / Skills / Harness 的解释交互。
- 完善智谱工作流的阅读锚点与节点说明。
- 完善缺陷系统视频、四模块索引和结果说明。
- 完善 Ganttodo 在线体验、示例输入和 iframe fallback。
- 保证四个项目入口、导航和外部说明区同级。
- 所有项目做好事实边界、脱敏和失败 fallback。

验证：四个项目都能回答“是什么 / 我做了什么 / 结果或边界”，没有任何一个被设计成默认主项目。

### Phase 4 — 全局交互

- 导航与章节状态。
- 自定义 cursor。
- 音乐播放器。
- hover 和 section motion。
- reduced-motion。

验证：动效强化关系和反馈；删除任一动效时能说明它是否仍有价值。

### Phase 5 — 验收与清理

- `npm run build`
- 浏览器逐断点验证。
- 键盘、音频、reduced-motion、触屏验证。
- 检查 console、溢出、加载失败和文案重复。
- 删除本次改动造成的无用样式、变量和组件。
- 输出 `docs/redesign-handoff.md`，记录完成项、未完成项、素材缺口和恢复命令。

---

## 14. 验收清单

### 内容

- [ ] MacBook 中有阿里云、智谱、智能缺陷、甘特待办四个并列文件夹。
- [ ] 智谱时间为 `2025.12 - 2026.05`。
- [ ] 阿里云时间为 `2026.06 - 至今`。
- [ ] 全国三等奖项目只出现在时间轴，不出现在 MacBook、项目导航或详情页。
- [ ] 第二页同时包含完整的 About、技术栈、邮箱、GitHub 和可选简历下载入口。
- [ ] 项目页结束后不再新增 Contact/Footer 页面。
- [ ] 爱好页没有被创建、接入或纳入本轮验收。
- [ ] 无虚构指标、职责和客户信息。

### 视觉

- [ ] 时间轴是独立长滚动页面，不是半屏卡片堆叠。
- [ ] 个人简介已从时间轴中分离。
- [ ] 普通内容不再大面积使用圆角矩形。
- [ ] 票根和 MacBook 保留且与新风格一致。
- [ ] 四个项目入口与导航同级，阿里云没有额外放大、徽章或默认打开。

### 交互

- [ ] Hover 状态清楚、有目的。
- [ ] 页面进入和章节切换有统一动效。
- [ ] 自定义 cursor 在桌面可用，在触屏/键盘环境不造成障碍。
- [ ] 音乐不自动播放，能控制进度、音量和静音。
- [ ] AG-UI 双向事件、Hermes、Skills、Memory、Harness 的关系可被看懂。

### 工程

- [ ] `npm run build` 成功。
- [ ] 关键页面没有 console error。
- [ ] 390px 到宽屏没有横向溢出。
- [ ] reduced-motion 有完整替代体验。
- [ ] 项目视频、iframe 和非首屏大图按需加载。
- [ ] `TransitionPage` 的事件契约和 MacBook 项目交互未回归。

---

## 15. Definition of Done

完成不是“页面看起来更炫”，而是同时满足：

1. 四个 MacBook 项目以同级入口和视觉权重并列呈现。
2. 跨工艺建模项目只作为时间轴经历保留，没有展开入口。
3. 每个展示区域都能独立回答访问者“这是什么、你做了什么、为什么重要”。
4. 长时间轴和四个项目详情都真正完成，不是占位；爱好页明确延期。
5. 动效、音乐和鼠标增加了探索感，但不妨碍阅读、性能和可访问性。
6. 网站在桌面和移动端经过真实浏览器验证。
7. 构建成功，交接文档能让下一次 Session 直接继续。
